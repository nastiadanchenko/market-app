spring:
  application:
    name: payment-api

  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: http://localhost:8085/realms/market


server:
  port: 8081

balance:
  value: 1000.00